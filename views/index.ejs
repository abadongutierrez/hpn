<!DOCTYPE html>
<html lang="en">
<head>
    <title>Today I feel like</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css"
          integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="/css/main.css"/>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"
            integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa"
            crossorigin="anonymous"></script>
    <script type="text/javascript">
        $(document).ready(function () {
            function showThankYou() {
                document.getElementById("overlay").style.width = "100%";

                setTimeout(function () {
                    document.getElementById("overlay").style.width = "0";
                }, 2000);
            }

            $('#goodLink').on('click', function () {
                $.post('/im/good').done(function() {
                    showThankYou();
                }).fail(function (error) {
                    console.log('Mood not logged: ' + error.statusText);
                });

            });
            $('#neutralLink').on('click', function () {
                $.post('/im/neutral').done(function() {
                    showThankYou();
                }).fail(function (error) {
                    console.log('Mood not logged: ' + error.statusText);
                });

            });
            $('#badLink').on('click', function () {
                $.post('/im/bad').done(function() {
                    showThankYou();
                }).fail(function (error) {
                    console.log('Mood not logged: ' + error.statusText);
                });
            });
        });

    </script>
</head>

<body>
<div id="overlay" class="overlay">
    <div class="overlay-content"><h1>Thank you!</h1></div>
</div>
<div class="office-info">Office User: <%= user %></div>
<div class="container-fluid">
    <div class="row">
        <div class="col-xs-12 col-sm-12 text-center">
            <h1>
                <small>Today</small>
                I feel
                <small>like</small>
            </h1>
        </div>
    </div>
    <div class="row">
        <div class="col-xs-6 col-sm-4">
            <button id="goodLink" style="background: url(<%= imgUrls.good %>) no-repeat;background-position: center;"
                    class="btn btn-default mood-button center-block"></button>
            <div class="caption text-center"><h3>Good</h3></div>
        </div>
        <div class="col-xs-6 col-sm-4">
            <button id="neutralLink"
                    style="background: url(<%= imgUrls.neutral %>) no-repeat;background-position: center;"
                    class="btn btn-default mood-button center-block"></button>
            <div class="caption text-center"><h3>Neutral</h3></div>
        </div>
        <div class="col-xs-6 col-sm-4">
            <button id="badLink" style="background: url(<%= imgUrls.bad %>) no-repeat;background-position: center;"
                    class="btn btn-default mood-button center-block"></button>
            <div class="caption text-center"><h3>Bad</h3></div>
        </div>
    </div>
</div>

</body>
</html>
